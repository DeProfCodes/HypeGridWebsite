// The current website has no testimonials section yet (placeholder only). This
// mock exists so the public content store has a source in mock mode / offline.
// Populate via the admin content manager → GET /api/public/testimonials.
export const testimonialsMock = [];
